#include <stdio.h>

int main(void)
{
	int number = 42;

	/* Using %d specifier to print integer value 'number'*/
	printf("Number using %%d: %d\n", number);

	/* Using %i specifier to print integer value 'number'*/
	printf("Number using %%i: %i\n", number);

	return (0);
}
